# RetainAI runtime dependencies.
# Versions are intentionally lower-bounded to keep the project reproducible
# while still allowing patch/security updates in local portfolio environments.

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.3.0

torch>=2.2.0
httpx>=0.26.0

# Useful for future DB-backed training/inference scripts.
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
